func fib(int n) -> int {
	if(n == 1 || n == 2) {
		return 1;
	} else {
		return fib(n - 1) + fib(n - 2);
	}
}


main {
	var result: int = fib(6);

	var i:int = 0;
	while(i != result) {
		print;
		i += 1;
	}
}

//888