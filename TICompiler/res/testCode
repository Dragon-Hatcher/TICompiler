func testFunc(int x, str y, bool z) -> int {
	return x + y + z;
}

main {
	var x:int = 1;
	x = x + 1;
	
	x = 1 + 2 * 3 + 6 / 2;
	
	//check if x is equals to 2 if so add three to it
	if(x == 2 && true) {
		int y = 3.156;
		x = x + y;
	}
}